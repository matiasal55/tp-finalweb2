<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css")
    body
        .container.my-5.mx-auto
            .d-flex
                img(src=`views/imagenes/logoMatanza.png` width=100)
                span(style="margin-left:auto;") Fecha: #{info['fecha_emision']}
            .d-sm-flex.align-items-center.justify-content-between.my-4
                h1(class="h3 mb-0 text-gray-800") Reporte de Proforma
            .row.mt-5
                .col-md-8
                    table.table.table-striped
                        tbody
                            each dato ,index in info
                                tr
                                    th(scope="row")= index.charAt(0).toUpperCase()+index.substr(1).replace("_"," ")
                                    if !dato
                                        td sin especificar
                                    else
                                        td= dato
                //.col-md-4
                //    img(src=`views/qr/proforma_${qr}.png` width=100)